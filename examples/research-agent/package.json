{
  "name": "@swarmapi/research-agent-example",
  "version": "0.0.0",
  "private": true,
  "description": "Demo agent that calls the gateway via @swarm-api/sdk on Base Sepolia.",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@swarm-api/sdk": "*",
    "@modelcontextprotocol/sdk": "^1.18.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2"
  }
}
