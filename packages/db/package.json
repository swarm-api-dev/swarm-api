{
  "name": "@agentpay/db",
  "version": "0.0.0",
  "private": true,
  "description": "Shared SQLite + Drizzle schema for providers, endpoints, payments, and marketplace listings.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
