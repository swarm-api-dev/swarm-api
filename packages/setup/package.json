{
  "name": "@agentpay/setup",
  "version": "0.0.0",
  "private": true,
  "description": "Interactive CLI: generate a Base wallet, fund it via Coinbase Onramp, and write a Claude Desktop MCP config block.",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "agentpay-setup": "src/index.ts"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "open": "^10.1.0",
    "viem": "^2.39.3"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2"
  }
}
